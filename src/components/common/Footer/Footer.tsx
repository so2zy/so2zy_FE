import styled from 'styled-components';

const Footer = () => {
  return <StyledWrapper>푸터</StyledWrapper>;
};

export default Footer;

const StyledWrapper = styled.div`
  border: 1px solid black;
  height: 1.5rem;
`;
